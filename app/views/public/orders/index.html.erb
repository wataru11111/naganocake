<div class="container">
 <h2>注文履歴一覧</h2>

<table class="table">
  <thead>
    <tr>
      <th>注文日</th>
      <th>配送先</th>
      <th>注文商品</th>
      <th>支払金額</th>
      <th>注文詳細</th>
    </tr>
  </thead>
  <tbody>
    <% @order.each do |order| %>
    <tr>
    <td>
      <%= order.created_at %>
   </td>
    <td>
      <%= order.address %>
    </td>
    <td>
      <%= order.name %>
  　</td>

  <td>
  <%= order.payment %>
  </td>

  <td>
  <%= link_to order_path(order.id) do %>
　<p>表示する</p>
　<% end %>
  </td>
  </tr>
    <% end %>
  </tbody>
</table>

